dependencies:
  pre:
    - echo y | android update sdk --no-ui --all --filter "android-31"
    - echo y | android update sdk --no-ui --all --filter "build-tools-31.0.0"
    - echo y | android update sdk --no-ui --all --filter "extra-android-m2repository"
    - echo y | android update sdk --no-ui --all --filter "extra-google-m2repository"

test:
  override:
    - ./gradlew assembleDebug
    - ./gradlew ktlintCheck